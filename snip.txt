<ul class="event-list">
                <li>
                    <time datetime="2014-07-20">
                        <span class="year">{{ event.date.strftime("%Y") }}</span>
                        <span class="day">{{ event.date.strftime("%d") }}</span>
                        <span class="month">{{ event.date.strftime("%B") }}</span>
                        <span class="time">{{ event.start.strftime("%H:%M") }} - {{ event.end.strftime("%H:%M") }}, {{event.date.strftime("%Y") }}</span>
                    </time>
                    <img alt="alttitle" src="https://farm4.staticflickr.com/3100/2693171833_3545fb852c_q.jpg" />
                    <div class="info">
                        <h2 class="title">{{ event.title }}</h2>
                        {% for user in users %}
                        {% if user.id == event.organizer %}
                        <p class="desc">{{ user.fname + ' ' + user.lname }}</p>
                        {%endif%}{%endfor%}
                        <p class="desc">{{ event.description }}</p>
                        <p class="stat">{{ event.status }}</p>
                    </div>
            </ul>